# 專案追蹤管理系統 (Project Tracker)

輕量化的個人專案追蹤管理系統，用於統一管理 WBS、Meeting Action Items 和問題追蹤。

## 📋 專案概述

### 特色功能

- ✅ **統一追蹤** - WBS 和 Action Items 集中管理
- 📊 **多維報表** - 快速產生不同對象的報告（團隊/長官/客戶）
- 🔗 **Impact 關聯** - 追蹤項目之間的依賴關係
- 📅 **三階段時程** - 計畫/調整/實際進度追蹤
- 💾 **本地部署** - 所有資料存放本機，無需網路

### 系統定位

- **使用者**: 個人使用（單一使用者）
- **平台**: Mac/Windows/Linux 本機部署
- **目標**: 輕量、快速、簡單

## 🛠 技術架構

| 層級 | 技術 | 版本 |
|------|------|------|
| 後端 | FastAPI | 0.104+ |
| 前端 | React + Vite | 18.2+ |
| 資料庫 | SQLite | 3.x |
| 資料處理 | pandas, openpyxl | - |
| 圖表 | Recharts | 2.10+ |
| 甘特圖 | frappe-gantt | 0.6+ |
| CSS | Tailwind CSS | 3.3+ |

## 📁 專案結構

```
project-tracker/
├── backend/                    # FastAPI 後端
│   ├── main.py                 # 應用入口
│   ├── config.py               # 設定檔
│   ├── database.py             # 資料庫連線
│   ├── init_db.py              # 資料庫初始化
│   ├── routers/                # API 路由
│   ├── services/               # 業務邏輯
│   └── models/                 # Pydantic 模型
│
├── frontend/                   # React 前端
│   ├── src/
│   │   ├── components/         # 共用元件
│   │   ├── pages/              # 頁面
│   │   ├── hooks/              # 自訂 Hooks
│   │   └── styles/             # 樣式
│   └── dist/                   # 打包後的靜態檔案
│
├── data/                       # 資料目錄
│   ├── project_tracking.db     # 主資料庫
│   └── backups/                # 備份目錄
│       ├── daily/
│       ├── weekly/
│       ├── monthly/
│       └── manual/
│
├── requirements.txt            # Python 依賴
├── README.md                   # 說明文件
└── .gitignore                  # Git 忽略設定
```

## 🚀 快速開始

### 系統需求

| 項目 | 需求 | 說明 |
|------|------|------|
| **作業系統** | Mac / Linux / Windows | 支援主流作業系統 |
| **Python** | 3.9+ | 建議使用 3.11 |
| **Node.js** | 18+ | 建議使用 LTS 版本 |
| **npm** | 10+ | 隨 Node.js 安裝 |
| **磁碟空間** | 500MB+ | 包含依賴套件 |
| **記憶體** | 2GB+ | 建議 4GB 以上 |

### 完整安裝步驟

#### 步驟 1: 克隆專案

```bash
git clone https://github.com/Emilyfan-learn/project-tracker.git
cd project-tracker
```

#### 步驟 2: 安裝後端依賴

```bash
# 建立 Python 虛擬環境（可選但建議）
python3 -m venv venv

# 啟動虛擬環境
# Mac/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# 安裝 Python 依賴套件
pip install -r requirements.txt
```

**主要依賴套件：**
- FastAPI - Web 框架
- uvicorn - ASGI 伺服器
- pandas - 資料處理
- openpyxl - Excel 處理
- pydantic - 資料驗證

#### 步驟 3: 安裝前端依賴

```bash
cd frontend
npm install
cd ..
```

**主要依賴套件：**
- React - UI 框架
- Vite - 建置工具
- Tailwind CSS - CSS 框架
- Recharts - 圖表庫
- frappe-gantt - 甘特圖

#### 步驟 4: 初始化資料庫

資料庫會在應用啟動時自動創建，但你也可以手動初始化：

```bash
python3 backend/init_db.py
```

這會創建 SQLite 資料庫檔案於 `data/project_tracking.db`

### 🎯 一鍵式啟動（推薦）

我們提供了便捷的啟動腳本，可以一鍵啟動前端和後端服務：

#### 啟動服務

```bash
./restart.sh
```

這個腳本會自動：
1. ✓ 停止現有的前端和後端進程
2. ✓ 檢查環境依賴（Python3, Node.js, npm）
3. ✓ 啟動後端服務（http://localhost:8000）
4. ✓ 啟動前端服務（http://localhost:5173）
5. ✓ 顯示服務狀態和日誌位置

#### 停止服務

```bash
./stop.sh
```

#### 查看服務日誌

```bash
# 查看後端日誌
tail -f logs/backend.log

# 查看前端日誌
tail -f logs/frontend.log
```

### 🛠 手動啟動（開發模式）

如果你不想使用一鍵式腳本，可以手動啟動：

#### 啟動後端

```bash
# 在專案根目錄
python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

後端服務：`http://localhost:8000`
- API 文件：`http://localhost:8000/docs`
- 健康檢查：`http://localhost:8000/health`

#### 啟動前端

```bash
# 開啟新終端機，進入 frontend 目錄
cd frontend
npm run dev
```

前端服務：`http://localhost:5173`

### 📦 生產部署

#### 方式 1: 使用打包的靜態文件

```bash
# 1. 打包前端
cd frontend
npm run build
cd ..

# 2. 啟動後端（會自動服務前端靜態文件）
python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

訪問 `http://localhost:8000` 即可使用完整應用

#### 方式 2: 使用 Docker（如需要）

```bash
# 構建映像
docker-compose build

# 啟動服務
docker-compose up -d
```

### ✅ 驗證安裝

啟動服務後，請確認以下項目：

1. **後端健康檢查**
   ```bash
   curl http://localhost:8000/health
   # 應該返回: {"status":"healthy","database":"True"}
   ```

2. **前端頁面**
   - 在瀏覽器打開 `http://localhost:5173`
   - 應該能看到專案追蹤系統首頁

3. **資料庫**
   ```bash
   ls -lh data/project_tracking.db
   # 應該看到資料庫文件
   ```

### 🔧 常見問題

#### Q1: 執行 `./restart.sh` 時顯示權限錯誤

```bash
# 賦予執行權限
chmod +x restart.sh stop.sh
```

#### Q2: Python 找不到模組

```bash
# 確保你已啟動虛擬環境
source venv/bin/activate  # Mac/Linux
venv\Scripts\activate     # Windows

# 重新安裝依賴
pip install -r requirements.txt
```

#### Q3: 前端無法連接後端 API

檢查 `frontend/vite.config.js` 中的代理設定：
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8000',
    changeOrigin: true,
  },
}
```

#### Q4: 資料庫表不存在

應用啟動時會自動創建表，但如果遇到問題：
```bash
# 手動初始化資料庫
python3 backend/init_db.py

# 或重啟服務
./restart.sh
```

#### Q5: 端口被占用

```bash
# 查找占用 8000 端口的進程
lsof -i :8000    # Mac/Linux
netstat -ano | findstr :8000  # Windows

# 停止進程
kill <PID>       # Mac/Linux
taskkill /PID <PID> /F  # Windows
```

### 📂 資料存放位置

- **資料庫**: `data/project_tracking.db`
- **備份**: `data/backups/`
- **日誌**: `logs/backend.log`, `logs/frontend.log`
- **PID 文件**: `logs/backend.pid`, `logs/frontend.pid`

## 📊 資料庫架構

系統使用 SQLite 資料庫，包含以下主要資料表：

- **projects** - 專案基本資料
- **tracking_items** - 追蹤項目（WBS/Action Items/Issues）
- **item_dependencies** - 項目依賴關係
- **schedule_changes** - 時程變更歷史
- **pending_items** - 待辦事項管理
- **issue_tracking** - 問題追蹤
- **issue_status_history** - 問題狀態歷史
- **notifications** - 通知管理

詳細 Schema 請參考 `backend/init_db.py`

## 💾 備份策略

系統支援自動備份機制：

- **每日備份** - 保留 30 天
- **每週備份** - 保留 12 週
- **每月備份** - 永久保存
- **手動備份** - 隨時可執行

備份檔案位於 `data/backups/` 目錄

## 🔧 API 文件

FastAPI 提供自動生成的 API 文件：

- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

## 📝 功能模組

### 核心功能

- ✅ WBS 項目管理
- ✅ Meeting Action Items 追蹤
- ✅ 問題管理（Issue Tracking）
- ✅ 待辦清單管理
- ✅ 甘特圖視圖
- ✅ 報表生成
- ✅ Excel 匯入/匯出

### 進階功能

- 📅 三階段時程追蹤（計畫/調整/實際）
- 🔗 項目依賴關係管理
- ⚠️ 逾期自動提醒
- 📊 進度偏差分析
- 🔔 通知系統

## 🤝 開發指南

### 程式碼風格

- Python: 遵循 PEP 8
- JavaScript: 使用 ESLint

### 提交規範

使用 Conventional Commits 格式：

```
feat: 新增功能
fix: 修復錯誤
docs: 文件更新
style: 格式調整
refactor: 重構
test: 測試
chore: 雜項
```

## 📄 授權

MIT License

## 👥 作者

專案追蹤管理系統開發團隊

## 📞 支援

如有問題或建議，請聯繫專案維護者。

---

**版本**: v2.0.0
**更新日期**: 2024-12-12
**狀態**: 開發中
